description "Consul service process"

start on (local-filesystems and net-device-up IFACE={{ consul.iface }})
stop on runlevel [!12345]

respawn

setuid {{consul.user }}
setgid consul {{ consul.group }}

exec consul agent -config-dir /etc/consul.d
